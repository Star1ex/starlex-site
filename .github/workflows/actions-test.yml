name: Actions Test

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Get the code
        uses: actions/checkout@v2

      - name: Say Hello World
        run: echo "Hello, World! Github Actions are working!"

      