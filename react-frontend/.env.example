VITE_SENTRY_DSN=CHANGE_ME_generate_with_openssl_rand_hex_32
# Default is subprotocol so access tokens are not placed in WebSocket URLs.
# Set to query only for legacy environments that cannot pass Sec-WebSocket-Protocol.
VITE_WS_AUTH_MODE=subprotocol
